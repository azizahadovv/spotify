import { createSlice } from "@reduxjs/toolkit"

const GetApiDate = createSlice({
    name: 'GetApiDate',
    initialState: {
        
    },
    reducers: {
       
    },
})

export const {} = GetApiDate.actions
export default GetApiDate.reducer